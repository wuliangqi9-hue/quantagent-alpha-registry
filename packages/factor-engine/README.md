# Factor Engine

Reusable factor computation module.

Initial implementation should be adapted from the existing
`crypto_factor_module` project.

## MVP Output

The factor engine should return chart-ready and selector-ready values:

- normalized factor scores;
- raw latest values;
- missing data flags;
- short factor explanations.

