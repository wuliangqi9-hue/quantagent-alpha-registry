[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "strategy-selector"
version = "1.0.0"
description = "Academic-research-backed strategy selector for QuantAgent Alpha Registry"
requires-python = ">=3.12"