# Agent Orchestrator

Production adaptation of QuantAgent's multi-agent idea for this Mantle project.

The reference QuantAgent project uses specialized agents for indicators,
patterns, trends, and final decision synthesis. This package keeps the same
shape, but implements deterministic reports so the hackathon demo remains
stable without requiring a separate LangGraph runtime.
