[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "agent-memory"
version = "1.0.0"
description = "FinMem-inspired lightweight memory store for QuantAgent Alpha Registry"
requires-python = ">=3.12"